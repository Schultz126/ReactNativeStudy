import { StyleSheet } from "react-native";

const selectStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },

    backgroundImage: {
        flex: 1,
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
    },

    UI: {
        backgroundColor: 'rgba(157, 142, 142, 0.4)',
        flex: 1,
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center' 
    }
})

export default selectStyles