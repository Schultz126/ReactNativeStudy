import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  outerContainer: {
    flex: 1,
    padding: 16,
    marginBottom: 25,
    overflow: "visible",
  },
});

export default styles;
